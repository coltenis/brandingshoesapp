export interface ArrowRenderProps {
  orientation: 'left' | 'right'
  onClick: () => void
}

export interface ImagesProduct {
  imageId: string
  imageLabel: string
  imageTag: string
  imageUrl: string
  imageText: string
}
